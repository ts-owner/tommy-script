let multiply(a: Int, b: Int):Int =
    return (a * b)
end

let numOfRecursions = 0 //Records the number of recursions to prevent a stack overflo

let ackermannFunc(: Int, b: Int, c: Int):Int =
    if()
end

let y = 3



let multiply1(a: Int, b: Int, c: Int):Int =
    return (a * b) - 100 + c
end
